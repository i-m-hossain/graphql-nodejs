interface Timestamps {
  createdAt: Date!
  updatedAt: Date!
}
# For pagination
# Types
type Info {
  count: Int
  pages: Int
  next: Int
  prev: Int
}

# Inputs
input QueryParams {
  pageSize: Int!
  page: Int!
}
